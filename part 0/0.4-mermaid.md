```mermaid
sequenceDiagram
    participant User
    participant Browser
    participant Server

    User->>Browser: Types note and clicks Save
    Browser->>Server: POST /notes with note data
    activate Server
    Server-->>Browser: Response with new note
    deactivate Server
    Note right of Browser: Page reloads, new note appears

```
